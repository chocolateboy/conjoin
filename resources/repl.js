Object.assign(globalThis, require('..'))

array = strings = ['foo', 'bar', 'baz', 'quux']
numbers = [1, 2, 3, 4]
single = array.slice(0, 1)
double = pair = array.slice(0, 2)
triple = array.slice(0, 3)
